<%= form_for @setting, :url => refinery.job_inquiries_admin_setting_path(@setting) do |f| %>

  <div class='field'>
    <span class='label_with_help'>
      <%= f.label :value, t('.job_inquiry_settings_value_name') %>
    </span>
    <%= f.text_field :value, :value => f.object.value, :class => "larger widest" %>
  </div>

  <p>
    <%= t('.hint') %>
  </p>
  <p>
    <%= t('.example') %>
  </p>

  <%= render '/refinery/admin/form_actions', :f => f,
             :continue_editing => false,
             :cancel_url => refinery.job_inquiries_admin_job_inquiries_path,
             :hide_delete => true  %>
<% end %>
